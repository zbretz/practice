//nodes are represented by integers
//paths connect nodes and are represented by lists of length two: [x,y]
//given a list of these paths, find all routes (connections of paths) that originate at node 0 and do not pass through a path more than once.

//example:
// paths = [[0,1], [1,2], [2,3], [1, 4]
// result = [0,1,2,3], [0,1,4]


var findPaths = function(arr){

}

